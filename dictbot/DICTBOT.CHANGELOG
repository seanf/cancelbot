12-14-06 2.2.0
Added thread support.  Queries are threaded so other users and queries aren't blocked.
ConfigParser is now used for the ini file.

2-8-05
Fixed private request so only answers when private trigger is '!define' and service is 'on'

Made option["deflimit"] an interger so the it works correctly

2-6-05
Initial release
